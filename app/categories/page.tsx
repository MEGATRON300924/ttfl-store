"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { api } from "@/lib/api-client";

type Category = { id: string; name: string; slug: string; children?: Category[] };
export default function CategoriesPage() { const [categories, setCategories] = useState<Category[]>([]); const [loading, setLoading] = useState(true); useEffect(() => { api.get<{ categories: Category[] }>("/api/categories").then((r) => setCategories(r.categories)).catch(() => setCategories([])).finally(() => setLoading(false)); }, []); return <main className="shell py-10 sm:py-14"><p className="text-xs font-bold uppercase tracking-[0.18em] text-ember-600">Shop by category</p><h1 className="mt-2 text-3xl font-bold text-graphite-950">All categories</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-graphite-600">Explore TTFL Store categories and discover products from independent sellers.</p>{loading ? <p className="mt-8 text-sm text-graphite-600">Loading categories…</p> : categories.length === 0 ? <div className="mt-8 rounded-card border border-dashed border-graphite-300 p-10 text-center text-sm text-graphite-600">No categories are available yet.</div> : <div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{categories.map((category) => <section key={category.id} className="rounded-card border border-graphite-200 bg-white p-5"><Link href={`/categories/${category.slug}`} className="text-lg font-bold text-graphite-900 hover:text-ember-600">{category.name}</Link>{category.children?.length ? <div className="mt-3 flex flex-wrap gap-2">{category.children.map((child) => <Link key={child.id} href={`/categories/${child.slug}`} className="rounded-full bg-cloud-100 px-3 py-1.5 text-xs font-medium text-graphite-700 hover:bg-ember-100 hover:text-ember-700">{child.name}</Link>)}</div> : <p className="mt-2 text-sm text-graphite-500">Browse products in this category.</p>}</section>)}</div>}</main>; }
