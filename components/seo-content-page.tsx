import Image from "next/image";
import Link from "next/link";

export function SeoContentPage({ eyebrow, title, description, sections, actions = [] }: { eyebrow: string; title: string; description: string; sections: { title: string; body: string }[]; actions?: { href: string; label: string }[] }) {
  return <main className="shell py-12 sm:py-16"><div className="overflow-hidden rounded-card border border-graphite-200 bg-white shadow-card"><div className="grid gap-8 p-6 sm:p-10 lg:grid-cols-[1fr_220px] lg:items-center"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-ember-600">{eyebrow}</p><h1 className="mt-3 text-3xl font-bold tracking-tight text-graphite-950 sm:text-4xl">{title}</h1><p className="mt-4 max-w-2xl text-base leading-7 text-graphite-600">{description}</p>{actions.length > 0 && <div className="mt-6 flex flex-wrap gap-3">{actions.map((action) => <Link key={action.href} href={action.href} className="rounded-card bg-ember-600 px-5 py-2.5 text-sm font-semibold text-white hover:bg-ember-700">{action.label}</Link>)}</div>}</div><div className="relative mx-auto h-44 w-44 overflow-hidden rounded-card border border-graphite-200 bg-cloud-100"><Image src="/icon.png" alt="TTFL Store" fill className="object-contain p-8" sizes="176px" /></div></div><div className="grid gap-5 border-t border-graphite-200 bg-cloud-50 p-6 sm:p-10 md:grid-cols-2">{sections.map((section) => <section key={section.title} className="rounded-card border border-graphite-200 bg-white p-5"><h2 className="text-base font-bold text-graphite-900">{section.title}</h2><p className="mt-2 text-sm leading-6 text-graphite-600">{section.body}</p></section>)}</div></div></main>;
}
