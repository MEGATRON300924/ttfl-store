"use client";
import Link from "next/link"; import { ArrowRight,Megaphone,ShieldCheck,Store } from "lucide-react"; import { useEffect } from "react"; import { api } from "@/lib/api-client";
export function SponsoredStoreCard({campaign}:{campaign:{id:string;storeName:string;storeSlug:string;storeLogoUrl:string|null;storeVerified:boolean}}){useEffect(()=>{void api.post("/api/ads/events",{campaignId:campaign.id,eventType:"IMPRESSION",visitorKey:typeof crypto!=="undefined"&&"randomUUID" in crypto?crypto.randomUUID():undefined})},[campaign.id]);return <Link href={`/store/${campaign.storeSlug}`} onClick={()=>{void api.post("/api/ads/events",{campaignId:campaign.id,eventType:"CLICK"})}} className="group flex items-center gap-4 rounded-card border border-graphite-200 bg-white p-4 hover:border-ember-500 dark:border-graphite-700 dark:bg-graphite-900"><span className="grid h-12 w-12 shrink-0 place-items-center overflow-hidden rounded-full bg-cloud-100 dark:bg-graphite-800">{campaign.storeLogoUrl?<img src={campaign.storeLogoUrl} alt="" className="h-full w-full object-cover"/>:<Store className="h-5 w-5 text-graphite-400"/>}</span><span className="min-w-0 flex-1"><span className="flex items-center gap-2"><span className="truncate font-semibold">{campaign.storeName}</span>{campaign.storeVerified&&<ShieldCheck className="h-4 w-4 shrink-0 text-verified-600"/>}</span><span className="mt-1 flex items-center gap-1 text-xs font-semibold text-ember-600"><Megaphone className="h-3.5 w-3.5"/>Sponsored</span></span><ArrowRight className="h-4 w-4 text-graphite-400 transition group-hover:translate-x-1"/></Link>}
