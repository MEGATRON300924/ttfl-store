"use client";
import { useState } from "react";import { Bell,Mail,Tag } from "lucide-react";import { api,ApiError } from "@/lib/api-client";import { useAuth } from "@/lib/auth-context";
export function NotifyAvailability({productId,productName}:{productId:string;productName:string}){const{user}=useAuth();const[email,setEmail]=useState(user?.email??"");const[whatsapp,setWhatsapp]=useState(user?.phone??"");const[type,setType]=useState<"BACK_IN_STOCK"|"PRICE_DROP">("BACK_IN_STOCK");const[targetPrice,setTargetPrice]=useState("");const[open,setOpen]=useState(false);const[saving,setSaving]=useState(false);const[message,setMessage]=useState<string|null>(null);const[error,setError]=useState<string|null>(null);async function submit(e:React.FormEvent){e.preventDefault();setSaving(true);setError(null);try{const result=await api.post<{message?:string}>(`/api/products/alerts/${productId}`,{type,email:email||undefined,whatsapp:whatsapp||undefined,targetPrice:type==="PRICE_DROP"?Number(targetPrice):undefined});setMessage(result.message??"Alert saved. We'll notify you when the product matches your alert.");setOpen(false);}catch(err){setError(err instanceof ApiError?err.message:"Could not save your alert.");}finally{setSaving(false);}}return <div className="rounded-card border border-graphite-200 bg-cloud-50 p-4 dark:border-graphite-700 dark:bg-graphite-900"><div className="flex items-start gap-3"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-full bg-ember-100 text-ember-700"><Bell className="h-5 w-5"/></span><div><p className="text-sm font-bold text-graphite-900 dark:text-white">Get notified</p><p className="mt-1 text-xs leading-5 text-graphite-600 dark:text-graphite-400">Choose a back-in-stock or price-drop alert for {productName}.</p></div></div>{!open?<button type="button" onClick={()=>setOpen(true)} className="mt-4 flex w-full items-center justify-center gap-2 rounded-card bg-graphite-900 px-4 py-3 text-sm font-semibold text-white"><Bell className="h-4 w-4"/>Create alert</button>:<form onSubmit={submit} className="mt-4 space-y-3"><div className="grid grid-cols-2 gap-2"><button type="button" onClick={()=>setType("BACK_IN_STOCK")} className={`rounded-card border px-3 py-2 text-xs font-semibold ${type==="BACK_IN_STOCK"?"border-ember-600 bg-ember-100":"border-graphite-200"}`}>Back in stock</button><button type="button" onClick={()=>setType("PRICE_DROP")} className={`rounded-card border px-3 py-2 text-xs font-semibold ${type==="PRICE_DROP"?"border-ember-600 bg-ember-100":"border-graphite-200"}`}><Tag className="mr-1 inline h-3 w-3"/>Price drop</button></div>{type==="PRICE_DROP"&&<input type="number" min="1" value={targetPrice} onChange={e=>setTargetPrice(e.target.value)} placeholder="Target price in ₦" required className="w-full rounded-card border border-graphite-200 bg-white px-3 py-2.5 text-sm"/>}<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" className="w-full rounded-card border border-graphite-200 bg-white px-3 py-2.5 text-sm"/><input type="tel" value={whatsapp} onChange={e=>setWhatsapp(e.target.value)} placeholder="WhatsApp number (optional)" className="w-full rounded-card border border-graphite-200 bg-white px-3 py-2.5 text-sm"/>{error&&<p className="rounded-[7px] bg-ember-100 px-3 py-2 text-xs text-ember-700">{error}</p>}<div className="flex gap-2"><button type="button" onClick={()=>setOpen(false)} className="flex-1 rounded-card border border-graphite-200 px-3 py-2.5 text-sm font-semibold">Cancel</button><button disabled={saving} className="flex-1 rounded-card bg-ember-600 px-3 py-2.5 text-sm font-semibold text-white">{saving?"Saving…":"Save alert"}</button></div></form>}{message&&<p className="mt-3 rounded-[7px] bg-verified-100 px-3 py-2 text-xs font-medium text-verified-700">{message}</p>}</div>}
